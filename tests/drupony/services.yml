services:
  symfony.component.filesystem:
    class:        %symfony.component.filesystem.class%
    arguments:    [@drupony, @service_container]
    properties:
      propertyInjection: @service_container
